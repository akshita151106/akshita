# AI Vision Lab

Real-time hand, face, eye and person detection in the browser (React + TypeScript + Tailwind + MediaPipe).

```
npm install
npm run dev
```

Deploy on Vercel/Netlify: build command `npm run build`, output directory `dist`.
